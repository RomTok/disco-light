Import('env idir_lib idir_inc')
libmmsbase = env.SharedLibrary('../../../lib/libmmsbase', Glob('*.cpp'), LIBS = "")
env.Install(idir_lib, libmmsbase)
env.Install(idir_inc + '/mmsbase', Glob('../../../inc/mmsbase/*.h'))
Export('libmmsbase')