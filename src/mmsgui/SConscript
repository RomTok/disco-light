Import('env')
Import('libmmsconfig')
env = env.Copy()
env.Append(LIBPATH = '../../../lib', LIBS = ['mmsconfig', 'mmstools'])
files = Glob('*.cpp')
files.append(Glob('*/*.cpp'))
libmmsgui = env.SharedLibrary('../../../lib/libmmsgui', files)
Depends(libmmsgui, libmmsconfig)
Export('libmmsgui')
